apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-outbound-https
spec:
  podSelector:
    matchLabels:
      network-policy: allow-outbound-https
  egress:
  - ports:
    - port: 443
      protocol: TCP